{% extends 'base.html.twig' %}

{% block title %}Hello AjouterController!{% endblock %}

{% block body %}
<section class="bg-ajouter">
  <div class="container">

    <div class="ajouter-form">
      {{ form_start(form) }}
        <h1>Ajouter un Manekineko</h1>
        {{ form_errors(form) }}
        {{ form_widget(form) }}
        <button type="submit">Enregistrer</button>
      {{ form_end(form) }}
    </div>
  </div>
</section>
{% endblock %}
